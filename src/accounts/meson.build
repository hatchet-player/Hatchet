# Hatchet accounts build file
#============================================================================


accounts_dirs = [
  'xmpp',
  #'google',
  #'hatchet',
  #'zeroconf'
]


foreach dir : accounts_dirs
    subdir(dir)
endforeach
